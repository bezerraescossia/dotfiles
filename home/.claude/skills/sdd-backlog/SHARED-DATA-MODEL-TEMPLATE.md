# Shared Data Model Template

Only write this file if the epic's Shared Entities section (in `epic.md`) is non-empty.

```markdown
# Shared Data Model: [EPIC NAME]

**Epic**: .spec/NN-epic-name/epic.md

Entities defined here are binding for every feature under this epic — a feature's own `data-model.md` (generated by `sdd-implement`'s Plan sub-stage) must reference these, not redefine them.

## [Entity Name]

**Represents**: [what it is, at a conceptual/business level]
**Used by**: [feature IDs from the backlog]

| Field | Type | Notes |
|---|---|---|
| [...] | [...] | [...] |

**Relationships**: [to other shared entities]
```
